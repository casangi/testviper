# Install the main branch versions of the components
# A clone of the components needs to exist before running
# the installation command. 
# It can be installed with pixi or directly with pip

# Local editable installs from cloned repos under external/
# The repos are synchronized and checked out to the desired
# branch/tag/commit by scripts/sync_components.sh
# -e external/toolviper[all]
# -e external/xradio[all]
# -e external/graphviper[all]
# -e external/astroviper[all]

-e external/xradio[all]
-e external/graphviper[all]
-e external/astroviper[all]
-e external/toolviper[all]

# Branch selection is done via scripts/sync_components.sh arguments
